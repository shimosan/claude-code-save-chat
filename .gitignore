# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# Local scratch (pre-public versions, personal backups)
scratch/

# Claude Code local settings (per-machine: permission allowlist, paths/usernames)
# Ignore .claude/ contents but keep the tracked library-maintenance CLAUDE.md
.claude/*
!.claude/CLAUDE.md

# Dropbox-synced, NOT published to GitHub (cross-machine private)
# Long-term dev records, insights
notes/
# Cross-machine shared private config (machine ledger etc.)
local/
# Cross-machine shared private raw logs (snapshots, apply history)
log/
